varying vec2 vUv;

void main()
{
  vec3 sphereNormal = normalize(position);
  vec3 spherePos = sphereNormal * 10000.0;

  vUv = uv;

  gl_Position = projectionMatrix * modelViewMatrix * vec4(spherePos, 1.0);
}
